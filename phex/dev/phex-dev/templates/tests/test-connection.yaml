apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "phex-dev.fullname" . }}-test-connection"
  labels:
{{ include "phex-dev.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "phex-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
